<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zunnorain.telcard_khokha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OperatorActivity.OperatorActivity" />
        <activity android:name=".CartActivity.CartActivity" />
        <activity android:name=".LoginSignup.LoginActivity" />
        <activity android:name=".LoginSignup.SignupActivity" />
        <activity android:name=".HistoryActivity.HistoryActivity" />
        <activity android:name=".PurchaseToBeDetails.PurchaseDeatils" />
        <activity android:name=".OptionsActivity.OptionsActivity" />
        <activity android:name=".BankDetailsActivity.BankDeatailsActivity"></activity>
    </application>

</manifest>